
# Cloud -- never commit plaintext credentials
credentials.json
credentials_clean.json
/tmp/

# Streamlit secrets (template is committed; real values are not)
dashboard/.streamlit/secrets.toml
dashboard/__pycache__/
